export default class UtilsService{

    //responce redirection
    responceData = async (data) =>{

        return data;
        // let responce = await data
        // if((responce.data.status === 0) || (responce.data.status === 1) || (responce.data.status === 2))
        //     return data;
        // else 
        // {
        //     window.location.replace("/error");
        // }
    }

}       